<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Aivance.

    Restricts cleartext traffic in production and applies
    certificate pinning to critical API endpoints.

    IMPORTANT: Before production deployment, enable certificate pinning
    by uncommenting the <pin-set> blocks below with real SHA-256 hashes.
    Generate them with:
      openssl s_client -connect HOST:443 -servername HOST </dev/null 2>/dev/null \
        | openssl x509 -pubkey -noout \
        | openssl pkey -pubin -outform der \
        | openssl dgst -sha256 -binary \
        | openssl base64
-->
<network-security-config>
    <!-- Default: HTTPS only, system trust anchors -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug domains: allow cleartext for local development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!--
        Certificate Pinning — DISABLED until real pins are configured.
        Uncomment and replace each pin digest with the actual base64-encoded
        SHA-256 hash of the server's SubjectPublicKeyInfo.
        Each domain should have at least 2 pins (primary + backup).

        Example:
        <domain-config>
            <domain includeSubdomains="true">api.openai.com</domain>
            <pin-set expiration="2029-12-31">
                <pin digest="SHA-256">BASE64_ENCODED_PIN_HERE</pin>
                <pin digest="SHA-256">BACKUP_PIN_HERE</pin>
            </pin-set>
        </domain-config>
    -->
</network-security-config>
